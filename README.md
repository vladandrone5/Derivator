# Derivator
A simple symbolic differentiator written in C#
- last implemented: the "^" case for number exponent
- last fixes: now for constants it prints '1' instead of ['1'], runs for majority of test
- still needs minor error handling